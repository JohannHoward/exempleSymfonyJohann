{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
<h1>La catégorie {{ categorie.Nom }}</h1>
<div class="shadow-lg p-3 mb-5 bg-body rounded">
    <div class="container">
        <table class="table table-striped table-hover ">
            <tbody>
                <tr class="table-active">
                    <th>Id</th>
                    <td>{{ categorie.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ categorie.Nom }}</td>
                </tr>
                <tr>
                    <th>NombreChaine</th>
                    <td>{{ categorie.NombreChaine }}</td>
                </tr>
                <tr>
                    <th>Prix</th>
                    <td>{{ categorie.Prix }}</td>
                </tr>
            </tbody>
        </table>
    </div>

    <div class="container ">
        <div class="row mt-4">
            <div class="col">
                {{ include('categorie/_delete_form.html.twig') }}
            </div>
            <div class="col">
                <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}"
                    class="w-100 mb-2 btn btn-lg rounded-4 btn-success"><i class="fas fa-edit"></i> Modifier</a>
            </div>
            <div class="col">
                <a href="{{ path('app_categorie_index') }}" class="w-100 mb-2 btn btn-lg rounded-4 btn-secondary"><i
                        class="fa fa-list"></i> Liste</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}