{% extends 'base.html.twig' %}

{% block title %}TypeChaine{% endblock %}

{% block body %}
    <h1>TypeChaine</h1>

    <div class="shadow-lg p-3 mb-5 bg-body rounded">
        <div class="container">
            <table class="table table-striped table-hover ">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ type_chaine.id }}</td>
                    </tr>
                    <tr>
                        <th>Type</th>
                        <td>{{ type_chaine.Type }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
        <div class="container ">
            <div class="row mt-4">
                <div class="col">
                    {{ include('type_chaine/_delete_form.html.twig') }}
                </div>
                <div class="col">
                    <a href="{{ path('app_type_chaine_edit', {'id': type_chaine.id}) }}"
                        class="w-100 mb-2 btn btn-lg rounded-4 btn-success"><i class="fas fa-edit"></i> Modifier</a>
                </div>
                <div class="col">
                    <a href="{{ path('app_type_chaine_index') }}" class="w-100 mb-2 btn btn-lg rounded-4 btn-secondary"><i
                            class="fa fa-list"></i> Liste</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
